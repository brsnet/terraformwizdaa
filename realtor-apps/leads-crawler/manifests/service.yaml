apiVersion: v1
kind: Service
metadata:
  name: leads-crawler
  namespace: realtor-apps
  labels:
    app: leads-crawler
spec:
  type: LoadBalancer
  selector:
    app: leads-crawler
  ports:
    - name: http
      port: 7002
      targetPort: 7002
      protocol: TCP
